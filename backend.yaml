meta:
  version: 0.0.1
  type: backend

permissions:
  fs:
    dirs: [ '/assets' ]

  db:
    - model: users
      permissions: 'rw'

  http:
    request: true

integrations:
  0: /integrations/test-integration-1/integration.yaml
  1: /integrations/test-integration-2/integration.yaml

steps:
  0:
    integration: 0
  1:
    integration: 1
  2:
    integration: null
